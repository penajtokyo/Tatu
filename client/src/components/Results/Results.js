import React from 'react';

const Results = () => {
    return (
        <div>
            <h4>Results Area</h4>
            {/* loop throught images array (of objects if has the descriptoin, style, placement
            and may each one into the card format*/}
        </div>
    );
};

export default Results;